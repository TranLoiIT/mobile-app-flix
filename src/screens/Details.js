import {Text, View} from 'react-native';
import { ViewContainer } from '../components/ViewContainer';

export function DetailsScreen() {
  return (
    <ViewContainer>
      <Text style={{fontSize: 32, color: 'red'}}>Details Screen</Text>
    </ViewContainer>
  );
}
